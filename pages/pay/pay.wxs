var filters = {
  toFix: function (value) {
    if(value<=0){
      return 0;
    }
    var dis = parseFloat(value);
    return dis.toFixed(2)//此处2为保留两位小数
  }
}
module.exports = {
  toFix: filters.toFix
}